import { createRipples } from "react-ripples";

export const RippleDark = createRipples({
  color: "rgba(0, 0, 0, 0.2)",
  during: 600,
});

export const RippleLight = createRipples({
  color: "rgba(38, 153, 247, 0.1)",
  during: 600,
});

export const RippleBlue = createRipples({
  color: "rgba(255, 255, 255 ,0.1)",
  during: 600,
});
